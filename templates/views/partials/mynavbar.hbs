<nav role="navigation">
  <ul class="nav we-nav">
    {{# each navLinks}}
    <li class="we-section-nav" {{#ifeq ../section key}}class="active"{{else}}{{/ifeq}} data-sectionindex="{{sectionIndex}}">
      <a href="{{ href }}"><span class="glyphicon {{icon}}"></span> {{ label }}</a>
    </li>
    {{/each}}
  </ul>
</nav>
